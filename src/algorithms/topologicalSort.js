

// returns an array of the sorted vertices in a connected graph, or false if no such array exists
function topologicalSort(numVertices, adjacencyList) {
	let store = {};

	for (let [u, v] of adjacencyList) {

	}
}